# LITELLM PROXY DEPENDENCIES #
anyio==4.15.1 # openai + http req.
httpx==0.28.1
openai==2.48.0  # openai req.
fastapi==0.128.8 # server dep
starlette==1.6.0 # starlette fastapi dep
backoff==2.2.1 # server dep
pyyaml==6.0.3 # server dep
uvicorn==0.39.0 # server dep
gunicorn==23.0.0 # server dep
fastuuid==0.14.0 # for uuid4
uvloop==0.22.1 # uvicorn dep, gives us much better performance under load
boto3==1.42.97 # aws bedrock/sagemaker calls
redis==8.1.0 # redis caching
prisma==0.15.0 # for db
mangum==0.22.0 # for aws lambda functions
pynacl==1.6.2 # for encrypting keys
google-cloud-aiplatform==2.1.3 # for vertex ai calls
google-cloud-iam==2.23.0 # for GCP IAM Redis authentication
google-genai==2.24.0
anthropic[vertex]==1.7.0
mcp==1.30.0 ; python_version >= "3.10"    # for MCP server
google-generativeai==0.8.6 # for vertex ai calls
async_generator==1.10.0 # for async ollama calls
langfuse==2.60.10 # for langfuse self-hosted logging
prometheus_client==0.26.0 # for /metrics endpoint on proxy
ddtrace==4.15.1      # for advanced DD tracing / profiling
orjson==3.11.5 # fast /embedding responses
polars==1.44.2 # for data processing
apscheduler==3.11.3 # for resetting budget in background
fastapi-sso==0.20.0 # admin UI, SSO
pyjwt[crypto]==2.14.0 ; python_version >= "3.9"
python-multipart==0.0.20 # admin UI
Pillow==12.3.0
azure-ai-contentsafety==1.0.0 # for azure content safety
azure-identity==1.25.3 ; python_version >= "3.9" # for azure content safety
azure-keyvault==4.2.0 # for azure KMS integration
azure-storage-file-datalake==12.25.0 # for azure buck storage logging
opentelemetry-api==1.25.0
opentelemetry-sdk==1.25.0
opentelemetry-exporter-otlp==1.25.0
grpcio>=1.62.3,<1.68.0  # Constraint for opentelemetry-exporter-otlp-proto-grpc to avoid resource exhausted bug (https://github.com/grpc/grpc/issues/38290)
sentry_sdk==2.69.2 # for sentry error handling
detect-secrets==1.5.0 # Enterprise - secret detection / masking in LLM requests
cryptography==44.0.1
tzdata==2026.4 # IANA time zone database
litellm-proxy-extras==0.4.9 # for proxy extras - e.g. prisma migrations
### LITELLM PACKAGE DEPENDENCIES
python-dotenv==1.2.1 # for env
tiktoken==0.14.0 # for calculating usage
importlib-metadata==7.1.0 # for random utils
tokenizers==0.22.2 # for calculating usage
click==8.1.8 # for proxy cli
rich==15.0.0 # for litellm proxy cli
jinja2==3.1.6 # for prompt templates
aiohttp==3.13.5 # for network calls
aioboto3==15.5.0 # for async sagemaker calls
tenacity==9.1.4  # for retrying requests, when litellm.num_retries set
pydantic>=2.11,<3 # proxy + openai req. + mcp
jsonschema==4.25.1 # validating json schema
websockets==15.0.1 # for realtime API
soundfile==0.13.1 # for audio file processing

########################
# LITELLM ENTERPRISE DEPENDENCIES
########################
litellm-enterprise==0.1.22
