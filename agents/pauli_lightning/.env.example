# THE PAULI EFFECT - Production Configuration
# Agent Lightning + LiteLLM + All Agents

# =============================================================================
# ENVIRONMENT VARIABLES
# =============================================================================

# Agent Lightning Store
PAULI_STORE_BACKEND=client
PAULI_STORE_URL=http://localhost:45993
PAULI_EXPERIMENT=pauli-effect-production

# Redis (for distributed training)
REDIS_URL=redis://localhost:6379

# LiteLLM Gateway
LITELLM_BASE_URL=http://localhost:4000
LITELLM_MASTER_KEY=sk-pauli-master-key

# LLM API Keys (set these!)
ANTHROPIC_API_KEY=your-anthropic-key
OPENAI_API_KEY=your-openai-key
GEMINI_API_KEY=your-gemini-key

# Default Model
PAULI_DEFAULT_MODEL=claude-sonnet-4-20250514

# Guardrails
PAULI_MAX_COST_PER_REQUEST=1.0
PAULI_MAX_REQUESTS_PER_MINUTE=60
PAULI_ENABLE_CONTENT_FILTER=true
PAULI_ENABLE_PII_FILTER=true

# Dashboard
PAULI_DASHBOARD_PORT=5173
PAULI_API_PORT=45993

# Logging
PAULI_LOG_LEVEL=INFO

# =============================================================================
# NOTES
# =============================================================================

# 1. Copy this file to .env and fill in your API keys
# 2. Start the Agent Lightning store: agl store --port 45993
# 3. Start LiteLLM: litellm --config deploy/litellm_config.yaml
# 4. Start the dashboard: cd agent-lightning/dashboard && npm run dev
# 5. Import Pauli: from pauli_lightning import Pauli
