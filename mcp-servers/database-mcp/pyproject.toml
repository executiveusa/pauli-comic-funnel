[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "database-mcp"
version = "1.0.0"
description = "Database MCP Server - Universal database operations for PostgreSQL, MySQL, MongoDB, Supabase"
authors = [{name = "The Pauli Effect", email = "dev@paulieffect.com"}]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.10"
keywords = ["mcp", "database", "postgresql", "mysql", "mongodb", "supabase", "ai-assistant"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Database"
]

dependencies = [
    "mcp",
    "starlette",
    "uvicorn",
    "asyncpg>=0.31.0",
    "aiomysql>=0.3.2",
    "motor>=3.7.1",
    "pydantic>=2.13.5",
]

[project.optional-dependencies]
dev = [
    "pytest>=9.1.1",
    "black>=26.5.1",
    "isort>=9.0.1",
    "mypy>=2.3.1"
]

[project.scripts]
database-mcp = "database_mcp:main"

[tool.black]
line-length = 100
target-version = ['py310']

[tool.isort]
profile = "black"
line_length = 100

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
addopts = ["-v", "--tb=short"]
