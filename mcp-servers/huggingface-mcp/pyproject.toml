[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "huggingface-mcp"
version = "1.0.0"
description = "HuggingFace MCP Server - ML model access and deployment"
authors = [{name = "The Pauli Effect", email = "contact@paulieffect.com"}]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.10"
keywords = ["mcp", "huggingface", "ml", "ai", "models", "transformers", "ai-assistant"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

dependencies = [
    "mcp",
    "starlette",
    "uvicorn",
    "aiohttp",
    "huggingface-hub>=0.20.0",
    "pydantic>=2.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "black>=22.0.0",
    "isort>=5.0.0",
    "mypy>=1.0.0"
]

[project.urls]
Homepage = "https://github.com/paulieffect/huggingface-mcp"
Repository = "https://github.com/paulieffect/huggingface-mcp"
Issues = "https://github.com/paulieffect/huggingface-mcp/issues"

[project.scripts]
huggingface-mcp = "huggingface_mcp.server:main"

[tool.black]
line-length = 100
target-version = ['py310']

[tool.isort]
profile = "black"
line_length = 100

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
addopts = ["-v", "--tb=short"]
